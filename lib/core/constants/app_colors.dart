import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF006A8A); // Tealish blue from header
  static const Color secondary = Color(0xFF008CBA);
  static const Color accent = Color(0xFF00A7E1);
  static const Color background = Color(0xFFF0F4F8); // Light greyish background
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color greyText = Colors.grey;
  static const Color darkGreyText = Color(0xFF4A4A4A);
  static const Color lightGrey = Color(0xFFE0E0E0);
  static const Color green = Colors.green;
  static const Color red = Colors.red;
  static const Color orange = Colors.orange; // For pending status
}
